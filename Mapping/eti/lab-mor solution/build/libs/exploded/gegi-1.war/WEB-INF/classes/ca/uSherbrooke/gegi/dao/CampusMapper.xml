<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ca.uSherbrooke.gegi.dao.CampusMapper" >


    <select id="selectCampus" resultType="Campus">
        SELECT *
        from projets3.v_campus
    </select>

    <select id="getIDCamp" resultType="Integer">
        SELECT id_campus
        from projets3.v_campus
        WHERE nom_campus = #{nom_campus}
    </select>

</mapper>